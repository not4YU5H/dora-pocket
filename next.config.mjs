/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "ceaseless-dove-416.convex.cloud",
            },
        ],
    },
};

export default nextConfig;
